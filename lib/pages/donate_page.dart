import 'package:flutter/material.dart';

class DonatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quyên góp'),
      ),
      body: Center(
        child: Text('Đây là trang quyên góp của ứng dụng'),
      ),
    );
  }
}
